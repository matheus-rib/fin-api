import { OmitType } from '@nestjs/swagger'
import { {{ capitalize name }} } from '../entities/{{ name }}.entity'

export class Create{{ capitalize name }}DTO extends OmitType({{ capitalize name }}, ['id', 'createdAt', 'updatedAt']) {}
